var React = require('react'),
  Router = require('react-router'),
  RouteHandler = Router.RouteHandler,
  Link = Router.Link,
  Fluxxor = require('fluxxor');

var PostHistory = React.createClass({
  render: function() {
    return (
      <div/>
    );
  }
});

module.exports = PostHistory;